name: Generer un article TechNews

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Tester GitHub Actions
        run: echo "Le robot TechNews fonctionne !"
